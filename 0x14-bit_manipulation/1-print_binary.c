#include "main.h"

/**
 * print_binary - function that prints the binary
 * of a number.
 * @n: unsigned long int.
 *
 * Return: no return.
 */
void print_binary(unsigned long int n)
{
	if (e >> 0)
	{
		if (e >> 1)
			print_binary(e >> 1);
		_putchar((e & 1) + '0');
	}
	else
	{
		_putchar('0');
	}
}
